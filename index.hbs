{{!< default}}
<section id="hero" {{#if @blog.cover}} style="background-image: url({{@blog.cover}}){{else}}no-cover{{/if}}">
  <div class="text">
    <h1>{{@blog.title}}</h1>
    <h2>{{@blog.description}}</h2>
  </div>
</section>
<section id="blog">
  {{> "loop"}}
</section>